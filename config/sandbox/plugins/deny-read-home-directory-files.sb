;; Deny read access to files within the user's home directory
(deny file-read* (subpath (param "HOME")))
;; Allow full read on directories so that path traversal and directory listing both work
(allow file-read* (require-all (subpath (param "HOME")) (vnode-type DIRECTORY)))
